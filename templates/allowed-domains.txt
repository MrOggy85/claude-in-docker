# Outbound allowlist — copy to allowed-domains.txt (gitignored) and rebuild the image.
# One entry per line; lines starting with # are ignored. An entry is a hostname
# ('.apex' = apex + subdomains), optionally narrowed by a path and a leading
# method list:  [METHOD[,METHOD] ]<host>[/path]  — e.g. "GET,HEAD api.github.com/repos".
# Full grammar: docs/egress-proxy.md#entry-syntax

# Claude Code API
api.anthropic.com
platform.claude.com

# Statsig (feature flags / telemetry)
statsig.anthropic.com
statsig.com

# Error reporting
sentry.io

# npm registry
registry.npmjs.org

# Node.js releases — for `nvm install <ver>` at runtime (tarballs + SHASUMS256.txt)
nodejs.org

# Python packages — for `uv` at runtime (index + wheels)
pypi.org
files.pythonhosted.org

# GitHub MCP
api.githubcopilot.com

# Atlassian MCP
mcp.atlassian.com
# OAuth dynamic client registration + token exchange endpoints (separate host)
cf.mcp.atlassian.com
